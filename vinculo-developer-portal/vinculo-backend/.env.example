# ─── VÍNCULO Backend — Variables de Entorno ───────────────
NODE_ENV=development
PORT=3000

# PostgreSQL
DATABASE_URL=postgresql://vinculo:vinculo_dev_2026@localhost:5432/vinculo?schema=public

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_URL=redis://localhost:6379

# JWT RS256
JWT_ALGORITHM=RS256
JWT_EXPIRATION=8h
JWT_ISSUER=vinculo-developer-portal

# SMTP — MailHog (desarrollo)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_FROM="VÍNCULO Portal" <noreply@vinculo.segurosbolivar.com>

# OTP
OTP_TTL_SECONDS=300
OTP_MAX_ATTEMPTS=3
OTP_BLOCK_DURATION_SECONDS=900

# Kong
KONG_ADMIN_URL=http://localhost:8001

# AI — Claude
ANTHROPIC_API_KEY=sk-placeholder-dev
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# CORS
CORS_ORIGIN=http://localhost:5173
